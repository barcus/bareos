name: 'push-bareos-readme'
description: 'Push Bareos readme to Docker Hub'
author: 'Barcus'
inputs:
  docker_pass:
    description: 'Docker Hub password'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.docker_pass }}
