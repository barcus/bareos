name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: checkout
    - uses: actions/checkout@v2
    - name: build docker image
    - run: |
        export BAREOS_APP='client'
        ./build.sh
